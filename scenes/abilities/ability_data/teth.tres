[gd_resource type="Resource" script_class="Ability" load_steps=2 format=3 uid="uid://beab01a"]

[ext_resource type="Script" path="res://scenes/abilities/ability.gd" id="1_ability"]

[resource]
script = ExtResource("1_ability")
id = &"teth"
display_name = "Overmind"
description = "Expands the mind's command over the battle. Grants combat-wheel slots equal to the points invested. Always active — it never needs to be equipped."
max_points = 3
kind = 4
target = 2
wheel_slots_per_point = 1
description_ranks = Array[String](["Always active — no need to equip. +1 combat-wheel slot.", "Always active — no need to equip. +2 combat-wheel slots.", "Always active — no need to equip. +3 combat-wheel slots."])
